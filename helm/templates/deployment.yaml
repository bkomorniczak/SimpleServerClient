apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tcp-server.fullname" . }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: {{ include "tcp-server.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "tcp-server.name" . }}
    spec:
      containers:
        - name: {{ include "tcp-server.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.service.port }}
